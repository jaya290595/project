<table>
  <tr>
    <th>Title</th>
    <th>Text</th>
  </tr>
  <tr id='content' >
    <td><%= post.title %></td>
    <td><%= post.text %></td>
    <td><%= link_to 'Edit', root_path(:post_id => post.id) %></td>
    <td><%= link_to "Delete" ,post, confirm: 'Are you sure?', method: :delete, remote: true %></td>
  </tr>

</table>